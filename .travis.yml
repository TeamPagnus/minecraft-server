language: python
before_install:
 - sudo apt update
 - sudo apt install firefox-geckodriver
 - sudo apt install openjdk-15-jre
 - sudo apt install screen
python:
  - 3.9
install:
  - pip install bs4 selenium ipaddress
script:
  - cd testing && python first.py
